# Contributing

1. Create a focused branch.
2. Add or update tests with behavioral changes.
3. Run `make lint` and `make test`.
4. Run `make eval` for changes affecting prompts, retrieval, or provider behavior.
5. Keep credentials and private datasets out of git.

The goal is reproducible engineering changes that can be evaluated before release.
