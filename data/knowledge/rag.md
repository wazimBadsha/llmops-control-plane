# Retrieval

A transparent lexical baseline makes retrieval inspectable. The system can show which document chunk matched the query and why, before the retrieval implementation is replaced with embeddings or a vector database.
